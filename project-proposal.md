# ES Markdown — Project Proposal

> เอกสารสรุปโปรเจกต์สำหรับตัดสินใจไปต่อ · 28 ส.ค. 2026
> อ้างอิง: `prompt-format-v1.md`, `instruction-forge-spec.md`, `markdown-master-wireframes-v1.md`

---

## 1. ปัญหา

คนทั่วไปเขียนคำสั่ง (prompt) ให้ AI ได้ผลลัพธ์แย่ ไม่ใช่เพราะพิมพ์ไม่เป็น แต่เพราะ **ไม่รู้ว่าต้องเขียนอะไรบ้าง** ให้ครบ ช่องว่างระหว่างคนที่ใช้ AI เก่งกับคนทั่วไปจึงไม่ใช่เรื่องทักษะการพิมพ์ แต่เป็นเรื่อง "รู้โครงสร้างที่ดี" กับ "ไม่รู้"

ผลคือคนที่ต้องการความช่วยเหลือมากที่สุด (มือใหม่) มักเลือกเขียนสั้นที่สุด ซึ่งเป็นทางที่ให้ผลลัพธ์แย่ที่สุด — เป็นปัญหาที่ยิ่งช่วยยิ่งไม่ถึงคนที่ต้องการ

## 2. สิ่งที่จะสร้าง

**ES Markdown** — เว็บแอปที่แปลง "การตอบคำถามในฟอร์ม" ให้กลายเป็นไฟล์ Markdown สั่ง AI ที่มีโครงสร้างครบ โดยผู้ใช้ไม่ต้องรู้ว่าโครงสร้างที่ดีหน้าตาเป็นอย่างไร — แอปเลือกโครงให้ ผู้ใช้แค่กรอกเท่าที่รู้ ช่องที่เหลือแอปเติมค่ามาตรฐานให้ครบเสมอ

4 โมดูลหลัก:

| โมดูล | ทำอะไร |
|---|---|
| **Home** | หน้าแรก อธิบายว่าแอปทำอะไร ทำไมต้อง Markdown |
| **Markdown workspace** | ฟอร์มนำทาง (guided form) ตามหัวข้องาน 5 แบบ ได้ผลลัพธ์เป็น Prompt / SKILL.md / Workflow.md |
| **Markdown Editor** | ตัวแก้ไข Markdown อิสระ พิมพ์เอง เห็นตัวอย่าง preview สด ๆ คู่กัน |
| **Agentic module** | ฟอร์มเฉพาะสำหรับสร้างไฟล์นิยาม AI agent (`AGENT.md`) |

## 3. ทำไมต้องทำตอนนี้

- มาตรฐานเนื้อหา (Prompt Format v1) และฟอร์ม 11 ช่องของ Agentic module ถูกออกแบบและทดสอบแนวคิดไว้แล้วใน `instruction-forge-spec.md` — งานส่วนเนื้อหาไม่ต้องเริ่มจากศูนย์
- ดีไซน์ระดับ high-fidelity ทั้ง 4 หน้าเสร็จแล้ว (ดู wireframes v1) พร้อมให้ทีมพัฒนาอ้างอิงได้ทันที
- โมเดล AI ส่วนใหญ่ประมวลผล Markdown ได้แม่นยำกว่าข้อความธรรมดา — จังหวะตลาดเหมาะกับการออกเครื่องมือที่ช่วยคนเขียน Markdown ให้ AI

## 4. กลุ่มเป้าหมาย

- **มือใหม่ที่ใช้ AI แต่ไม่รู้จะเขียน prompt อย่างไร** — กลุ่มหลัก ต้องได้ผลลัพธ์ที่ดีแม้กรอกน้อย
- **ผู้ใช้ระดับกลางถึงสูง** ที่อยากได้ความเร็ว ไม่ต้องพิมพ์โครง section เองทุกครั้ง
- **คนที่ต้องการสร้าง AI agent** ให้ทีม/โปรเจกต์ตัวเอง แต่ไม่คุ้นกับการเขียน AGENT.md

## 5. เป้าหมาย

1. ผู้ใช้กรอกฟอร์มน้อยที่สุด (เท่าที่รู้) ก็ยังได้คำสั่งที่มีโครงสร้างครบทุก section เสมอ
2. ลดจำนวนรอบที่ต้องแก้คำสั่งซ้ำจนกว่าจะได้ผลลัพธ์ที่ใช้ได้ — จากค่าเฉลี่ยเดิม (พิมพ์ดิบ) 4-5 รอบ ให้เหลือ 1-2 รอบ
3. ส่งมอบ 4 โมดูลตามดีไซน์ที่มีอยู่ โดยไม่เพิ่มฟีเจอร์เกินขอบเขตที่ตกลงกันไว้

## 6. ขอบเขต (Scope)

**อยู่ในขอบเขตรอบนี้**
- 4 โมดูล: Home, Markdown workspace, Markdown Editor, Agentic module
- ผลลัพธ์ 3 รูปแบบจาก workspace (Prompt `.md`, `SKILL.md`, Workflow `.md`) และ 1 รูปแบบจาก Agentic module (`AGENT.md`)
- มุมมอง Outline (ฟอร์ม) เป็นค่าเริ่มต้น
- สวิตช์ภาษาไทย/อังกฤษ และธีมสว่าง/มืด พร้อมจำค่าไว้ในเครื่อง

**นอกขอบเขตรอบนี้** (ดูเหตุผลใน specification.md § Non-Goals)
- มุมมอง Canvas/กราฟโหนดแบบลากวางเต็มรูปแบบ (ทำ static teaser ก่อน)
- การเซฟ/exportกราฟเป็น JSON
- บัญชีผู้ใช้ / ประวัติที่เก็บถาวรข้ามอุปกรณ์
- การเชื่อมต่อ AI จริงเพื่อรันคำสั่งที่สร้าง (แอปแค่ "สร้างไฟล์" ไม่ "รันคำสั่ง")

## 7. ความเสี่ยง / สิ่งที่ยังไม่พิสูจน์

สมมติฐานหลักของทั้งโปรเจกต์ยังไม่ได้ทดสอบ: **"ฟอร์มช่วยให้ prompt ดีขึ้นจริง"** อาจไม่จริงเสมอไป เพราะสำหรับบางคน prompt ที่กำกวมสะท้อนความคิดที่ยังไม่ชัดเจน ซึ่งฟอร์มแก้ให้ไม่ได้

**ก่อนลงทุนสร้างเพิ่ม ต้องทำ blind test ก่อน**: เอาโจทย์กำกวม 5 ข้อ ยิงพิมพ์ดิบเทียบกับยิงผ่านแอป แล้ววางผลเทียบแบบไม่บอกว่าอันไหนคืออันไหน ถ้าความต่างไม่ชัดเจน = ต้องกลับไปคิดทิศทางใหม่ก่อน

## 8. คำถามที่ต้องตัดสินใจก่อนเริ่มสร้างจริง

| คำถาม | ใครตอบ |
|---|---|
| ยืนยันธีมใหม่ (navy+cream+pink) เป็นตัวสุดท้าย และเลิกใช้ธีมส้ม/ครีมเดิมหรือไม่ | P'Earth |
| จะรันการทดสอบ blind test (ข้อ 7) เมื่อไร ก่อนหรือหลังสร้าง MVP | P'Earth |
| ต้องเปลี่ยนชื่อ Claude Project จาก "MarkdownMaster" เป็น "ES Markdown" ไหม | P'Earth |

## 9. ขั้นต่อไป

1. P'Earth ยืนยันขอบเขตและคำถามในข้อ 8
2. เริ่มสร้าง MVP ตาม `specification.md`
3. รัน blind test ตามข้อ 7 ก่อนขยายฟีเจอร์รอบถัดไป
