# ES Markdown — Specification

> Workflow และฟังก์ชันของแต่ละโมดูล · 28 ส.ค. 2026
> ธีม: dark navy + cream + light pink, claymorphism · Fraunces (display) + Sora (UI) + JetBrains Mono (code)
> อ้างอิง: `prompt-format-v1.md` (มาตรฐานเนื้อหา), `instruction-forge-spec.md` (ฟอร์ม/ผลลัพธ์), `markdown-master-wireframes-v1.md` (ดีไซน์)

---

## 0. หลักการร่วมของทุกโมดูล

มาจาก Prompt Format v1 — ใช้ทุกที่ที่แอปสร้างคำสั่ง (ไม่ใช่โหมดเอกสาร/note):

1. คำสั่งที่ออกจากแอปมีครบทุก section เสมอ ไม่ว่าผู้ใช้จะกรอกกี่ช่อง
2. ช่องที่ไม่ได้กรอก เติมด้วย **ค่ามาตรฐาน** ที่เขียนไว้ล่วงหน้า ไม่ตัดทิ้ง
3. ทุกคำสั่งปิดท้ายด้วย guardrail คงที่ + follow-up pack 3 ข้อ (อยู่ใต้เส้นคั่น ห้ามส่งพร้อมคำสั่งหลัก)

---

## 1. โมดูล Home

### Workflow
ผู้ใช้เปิดแอป → เห็น hero + คำอธิบาย → เลื่อนดูเหตุผล/ตัวอย่าง → กดเข้าโมดูลที่ต้องการ (Markdown / Editor / Agentic)

### หน้าที่ (ไม่มีการประมวลผลข้อมูล เป็นหน้าสถิตเป็นหลัก)
- แสดง headline + ช่องค้นหา/เริ่มต้นที่พาไปโมดูล Markdown workspace
- แสดงตัวอย่าง live-typing ของ Markdown preview (เดโม อธิบายว่าแอปทำอะไร)
- แสดงแถบโลโก้โมเดล AI ที่รองรับ (ChatGPT/Gemini/Claude/Grok/Mistral/Perplexity) — เป็นข้อมูลนิ่ง
- การ์ด "ทำไมต้อง ES Markdown" 3 ใบ
- สถิติ/เหตุผลว่าทำไม AI อ่าน Markdown ได้ดีกว่า
- คำรับรอง (testimonial) 3 กลุ่มผู้ใช้
- ปุ่ม navigation ไปยัง Markdown / Editor / Agentic

### Non-goals ของโมดูลนี้
- ไม่มี logic คำนวณอะไร เป็นหน้าการตลาด/ทางเข้าเท่านั้น

---

## 2. โมดูล Markdown workspace

### Workflow
```
เลือกหัวข้องาน (5 แบบ)
        │
        ▼
แอป seed ฟอร์ม 9 ช่อง พร้อมค่ามาตรฐานใต้ทุกช่อง (Outline view)
        │
        ▼
ผู้ใช้กรอกเท่าที่รู้ (0–9 ช่อง) ─── preview อัปเดตสด ทุกครั้งที่พิมพ์
        │
        ▼
เลือกรูปแบบผลลัพธ์: Prompt .md / SKILL.md / Workflow .md
        │
        ▼
กด Copy → ได้ข้อความ Markdown ครบ section พร้อม guardrail + follow-up pack
```

### หัวข้องาน (5 แบบ)
Software Engineering/Full Stack · Research/Explore/Brainstorm · Content/Script/Briefing · Everyday tasks · (สร้าง Agent → ไปที่โมดูล Agentic แทน)

### ฟังก์ชันหลัก

| ฟังก์ชัน | รายละเอียด |
|---|---|
| **เลือกหัวข้องาน** | สลับหัวข้อได้ตลอด ข้อมูลแต่ละหัวข้อเก็บแยกกัน ไม่ทับกัน |
| **กรอกฟอร์ม (Outline)** | 9 ช่องกรอกเรียงตามลำดับ section แต่ละช่องมีค่ามาตรฐานแสดงใต้ช่อง (สีจาง) ให้เห็นว่าถ้าไม่กรอกจะได้อะไรแทน |
| **Live preview** | Markdown ผลลัพธ์อัปเดตทันทีที่พิมพ์ ไม่ต้องกดปุ่ม |
| **สลับรูปแบบผลลัพธ์** | Prompt `.md` / `SKILL.md` / Workflow `.md` — สลับได้โดยไม่ต้องกรอกใหม่ (ใช้ข้อมูลชุดเดียวกัน) |
| **มิเตอร์ความครบ** | แสดง "เขียนเอง X / ทั้งหมด 9" เป็นชิป ให้เห็นว่ากรอกไปเท่าไรแล้ว |
| **ประวัติ (history)** | รายการงานที่เคยกรอกไว้ในเซสชันนี้ เลือกกลับมาดู/แก้ต่อได้ |
| **คัดลอกผลลัพธ์** | ปุ่ม Copy คัดลอก Markdown ที่ preview อยู่ทั้งหมด |
| **Ask for a pattern (ตัวช่วย AI)** | ปุ่มลอย เสนอแนะการกรอกจากสิ่งที่กรอกไปแล้ว — **P1 ไม่บังคับสำหรับ v1** |

### Acceptance criteria (ตัวอย่าง — เลือกหัวข้องาน + กรอกบางส่วน)
- [ ] เลือกหัวข้อแล้ว ฟอร์ม 9 ช่องต้องมาพร้อมค่ามาตรฐานครบทุกช่องทันที (ไม่มีช่องว่างเปล่า)
- [ ] กรอกแค่บางช่อง แล้วดู preview: ต้องมีครบ 9 section เสมอ, ช่องที่ไม่กรอกต้องใช้ค่ามาตรฐาน ไม่ใช่ข้อความว่าง
- [ ] สลับหัวข้องานแล้วกลับมาหัวข้อเดิม: ข้อมูลที่กรอกไว้ต้องยังอยู่ครบ
- [ ] สลับรูปแบบผลลัพธ์ (Prompt → SKILL.md): เนื้อหาที่กรอกเองต้องตรงกัน โครง section เปลี่ยนตามรูปแบบใหม่
- [ ] ทุกผลลัพธ์ต้องปิดท้ายด้วย guardrail คงที่ + follow-up pack 3 ข้อใต้เส้นคั่น

### Non-goals ของโมดูลนี้
- ไม่มีมุมมอง Canvas/กราฟโหนดแบบเต็ม (อยู่นอกขอบเขต v1 — ดู proposal § 6)
- ไม่มีการเซฟข้ามเซสชัน/อุปกรณ์ (ประวัติอยู่ในเซสชันปัจจุบันเท่านั้น)

---

## 3. โมดูล Markdown Editor

### Workflow
```
เปิดแท็บ Editor → เห็นตัวอย่างเนื้อหาเริ่มต้น (weekly sync notes)
        │
        ▼
พิมพ์/แก้ไขข้อความฝั่งซ้าย (plain text + syntax แบบ Markdown)
        │
        ▼
ฝั่งขวาเรนเดอร์ผลลัพธ์แบบสดคู่กันตลอดเวลา
        │
        ▼
ใช้ toolbar จัดรูปแบบแทนการพิมพ์ syntax เอง (ถ้าต้องการ)
```

### ฟังก์ชันหลัก

| ฟังก์ชัน | รายละเอียด |
|---|---|
| **สองพาเนลคู่กัน** | ซ้าย = plain text แก้ไขได้, ขวา = live rendered preview (อ่านอย่างเดียว) |
| **Toolbar จัดรูปแบบ** | Block-style dropdown (H1-H3/Paragraph), Bold/Italic/Strikethrough/Inline-code, bullet/numbered/checklist list, Link/Image/Table/Quote/Code-block/Divider |
| **Undo/Redo** | ย้อนกลับ/ทำซ้ำการแก้ไขข้อความ |
| **เนื้อหาตัวอย่างเริ่มต้น** | เอกสาร "weekly sync notes" โชว์ header, bold, italic, list, table, blockquote, inline code ที่เรนเดอร์ถูกต้อง — ใช้เป็น onboarding ในตัว |
| **คัดลอกผลลัพธ์** | คัดลอก Markdown ดิบ (ฝั่งซ้าย) ทั้งหมด |

### Acceptance criteria
- [ ] พิมพ์ syntax Markdown ฝั่งซ้าย (เช่น `**bold**`, `# Header`, `| table |`) แล้วฝั่งขวาต้องเรนเดอร์ถูกต้องทันที ไม่ต้องกดปุ่ม
- [ ] กดปุ่ม toolbar (เช่น Bold) ขณะเลือกข้อความ: ต้องแทรก/ครอบ syntax ที่ถูกต้องรอบข้อความที่เลือก
- [ ] Undo หลังพิมพ์ต้องย้อนกลับทีละ action ไม่ใช่ทีละตัวอักษร (grouped)
- [ ] เปิดโมดูลครั้งแรกต้องเห็นเนื้อหาตัวอย่างที่มีครบทุกองค์ประกอบ (header/bold/italic/list/table/quote/inline-code) เรนเดอร์ถูกทุกจุด

### Non-goals ของโมดูลนี้
- ไม่เชื่อมโยงกับฟอร์มใน Markdown workspace (คนละโหมดกัน ไม่มีปุ่ม "เปิดใน Editor" ใน v1)
- ไม่มีการเซฟไฟล์ถาวร (คัดลอกออกไปใช้เองนอกแอป)

---

## 4. โมดูล Agentic module

### Workflow
```
เปิดแท็บ Agentic → เห็นรายการ agent ที่เคยสร้าง (sidebar) + ปุ่มสร้างใหม่
        │
        ▼
กรอกฟอร์ม 11 ช่อง แบ่ง 3 กลุ่ม: Identity / Behavior / Guardrails
   ทุกช่องมีค่ามาตรฐานรอไว้ (เข้มกว่าปกติ โดยเฉพาะ Tools + Guardrails)
        │
        ▼
Live preview ฝั่งขวา: AGENT.md พร้อม YAML frontmatter (name, description, tools)
        │
        ▼
(ตัวเลือก) เลื่อนลงดู Workflow graph — teaser แบบ static ยังไม่ persist
        │
        ▼
กด Copy → ได้ AGENT.md ครบ
```

### ฟังก์ชันหลัก

| ฟังก์ชัน | รายละเอียด |
|---|---|
| **รายการ Agent (sidebar)** | แสดง agent ที่สร้างไว้ในเซสชันนี้ เลือกกลับมาแก้ได้ |
| **ฟอร์ม 11 ช่อง แบ่ง 3 กลุ่ม** | Identity (ชื่อ, บทบาท, คำสั่งหลัก), Behavior (ใช้เมื่อไร, input, tools, ขั้นตอน, น้ำเสียง), Guardrails (ขอบเขต/ส่งต่อคน, output/handoff, เกณฑ์สำเร็จ) |
| **ค่ามาตรฐานเข้มพิเศษ** | โดยเฉพาะ Tools และ Guardrails: ดีฟอลต์คือ "อ่าน/ค้นหาเท่านั้น ห้ามทำสิ่งที่ย้อนกลับไม่ได้โดยไม่ขออนุญาต" — กันไม่ให้ agent ที่สร้างจากฟอร์มเปล่าเป็นอันตราย |
| **Live preview AGENT.md** | แสดงพร้อม YAML frontmatter (`name`, `description`, `tools`) อัปเดตสดตามฟอร์ม |
| **Workflow graph (teaser)** | กระดาน node แบบ Role → Step → Decision → Output วาดด้วย SVG bezier — แสดงผลอย่างเดียว ยังไม่ persist ไม่ผูกกับข้อมูลฟอร์มจริง |
| **คัดลอกผลลัพธ์** | คัดลอก AGENT.md ทั้งไฟล์ |

### Acceptance criteria
- [ ] เปิดฟอร์มใหม่โดยไม่กรอกอะไรเลย แล้วดู preview: ต้องได้ `AGENT.md` ที่มี frontmatter ครบ + ทุก section ใช้ค่ามาตรฐาน โดย tools ต้อง default เป็น read-only/ห้ามทำสิ่งย้อนกลับไม่ได้
- [ ] กรอกช่อง "ชื่อ Agent" แล้ว preview: `name:` ใน frontmatter ต้องอัปเดตตาม
- [ ] สร้าง agent ใหม่จาก sidebar: ต้องไม่ทับข้อมูล agent ที่มีอยู่แล้วในรายการ
- [ ] Workflow graph section ต้องระบุชัดเจนในหน้า UI ว่าเป็นตัวอย่าง/ยังไม่บันทึกจริง (ป้องกันผู้ใช้เข้าใจผิดว่าแก้กราฟแล้วมีผลกับ AGENT.md)

### ข้อจำกัดที่รู้ตัว (สืบทอดจาก instruction-forge-spec.md)
- ชื่อ Agent ที่เป็นภาษาไทยจะกลายเป็น `name:` ภาษาไทยตรง ๆ ซึ่งใช้กับหลาย runtime จริงไม่ได้ — ต้องแก้เองก่อนใช้งานจริง (ยังไม่มี transliteration อัตโนมัติใน v1)
- `tools:` ใน AGENT.md เป็นประโยคยาว ไม่ใช่ list ชื่อ tool — ต้องแก้มือถ้าจะเอาไปใช้กับ runtime ที่ parse ฟิลด์นี้ตรง ๆ
- Workflow graph ไม่ persist ปิดแท็บแล้วหาย

### Non-goals ของโมดูลนี้
- ไม่มี drag-and-drop เชื่อมโหนดจริงใน v1 (เป็น static teaser)
- ไม่มี export/import JSON ของกราฟ
- ไม่แตก Decision node เป็นเงื่อนไข if/else จริง

---

## 5. ฟังก์ชันร่วมข้ามโมดูล (Global)

| ฟังก์ชัน | รายละเอียด |
|---|---|
| **สวิตช์ภาษา ไทย/EN** | Segmented toggle มุมขวาบน เปลี่ยนพร้อมกันทั้ง UI, เนื้อหาเทมเพลต, ค่ามาตรฐาน, follow-up pack, และ Markdown ที่ออก — ข้อความที่ผู้ใช้พิมพ์เองไม่ถูกแปล |
| **สวิตช์ธีมสว่าง/มืด** | ไอคอนพระจันทร์/พระอาทิตย์ ตามค่าที่เลือกไว้ล่าสุด หรือตามระบบถ้ายังไม่เคยเลือก |
| **จำค่าไว้ในเครื่อง** | ภาษา+ธีม เก็บใน `localStorage` (ครอบ try/catch ทั้งอ่าน-เขียน) — ถ้าเบราว์เซอร์บล็อก storage แอปยังทำงานได้ปกติ แค่ไม่จำค่า |
| **Responsive** | จอแคบ: icon rail ย้ายเป็นแถบบน, preview เลื่อนลงไปอยู่ล่างสุด |

### Acceptance criteria
- [ ] สลับภาษาแล้ว: label, ชื่อช่อง, ค่ามาตรฐาน, guardrail, follow-up pack ต้องเปลี่ยนภาษาทั้งหมดพร้อมกัน แต่ข้อความที่ผู้ใช้พิมพ์เองต้องไม่ถูกแตะต้อง
- [ ] ปิดเบราว์เซอร์แล้วเปิดใหม่: ภาษาและธีมต้องเป็นค่าล่าสุดที่เลือกไว้ (ถ้า storage ใช้งานได้)
- [ ] บล็อก localStorage (เช่น private mode บางเบราว์เซอร์): แอปต้องยังใช้งานได้ปกติ ไม่มี error ค้าง

---

## 6. ผลลัพธ์ทั้ง 4 แบบ (สรุปโครง)

| แบบ | ใช้ที่โมดูล | โครง section |
|---|---|---|
| Prompt `.md` | Workspace | เป้าหมาย → บริบท → ข้อมูลตั้งต้น → สิ่งที่ต้องมี → ข้อจำกัด → รูปแบบผลลัพธ์ → เกณฑ์ว่าดีพอ → guardrail → follow-up pack |
| `SKILL.md` | Workspace | YAML frontmatter (name, description) → เมื่อไรควรใช้ → ข้อมูลที่ต้องมี → ขั้นตอน → ข้อจำกัด → รูปแบบผลลัพธ์ → เกณฑ์ว่าดีพอ → guardrail |
| Workflow `.md` | Workspace | วัตถุประสงค์ → บริบท → อินพุต → ลำดับงาน (checkbox ต่อขั้น พร้อมบรรทัดยืนยันก่อนไปขั้นถัดไป) → ข้อจำกัด → ผลลัพธ์ที่ส่งมอบ → ถือว่าเสร็จเมื่อ → guardrail |
| `AGENT.md` | Agentic | YAML frontmatter (name, description, tools) → บทบาท → คำสั่งหลัก → ใช้เมื่อไร → ข้อมูลที่ได้รับ → เครื่องมือที่ใช้ได้ → ขั้นตอนการทำงาน → ขอบเขต/ส่งต่อคน → น้ำเสียง → ผลลัพธ์/การส่งต่อ → เกณฑ์สำเร็จ → guardrail |

Guardrail คงที่ (ใช้ทุกแบบ):
> ถ้าข้อมูลข้างบนไม่พอที่จะทำงานนี้ให้ดี ให้ถามกลับไม่เกิน 3 คำถามที่สำคัญที่สุดก่อน แล้วรอคำตอบ อย่าเดาแล้วทำต่อ สิ่งไหนที่ไม่มีข้อมูลรองรับ ให้บอกตรง ๆ ว่าไม่รู้ ห้ามแต่งขึ้นมา

---

## 7. Non-Goals ของ v1 (ภาพรวมทั้งแอป)

| Non-goal | เหตุผล |
|---|---|
| มุมมอง Canvas/กราฟโหนดแบบลากวางเต็มรูปแบบ | ซับซ้อนเกินจำเป็นสำหรับ v1, Outline เพียงพอสำหรับ 80% ของผู้ใช้ที่กลัวกระดานเปล่า |
| Export/Import JSON ของกราฟ | ยังไม่มี use case ชัดเจนจนกว่าจะพิสูจน์สมมติฐานหลักก่อน (ดู proposal § 7) |
| บัญชีผู้ใช้ / เก็บประวัติข้ามอุปกรณ์ | เพิ่มความซับซ้อนด้าน auth/backend ที่ไม่จำเป็นต่อการพิสูจน์คุณค่าหลักของแอป |
| เชื่อมต่อรันคำสั่งกับ AI จริงในแอป | ขอบเขตแอปคือ "ช่วยเขียนคำสั่ง" ไม่ใช่ "เป็น AI client" — ผู้ใช้คัดลอกไปใช้เอง |
| Decision node แบบ if/else จริง | ต้องรอ Canvas เต็มรูปแบบก่อน ตอนนี้ยังเป็นแค่ลำดับ (sequence) ไม่ใช่ผังตัดสินใจ |
| Transliteration ชื่อไทย → อังกฤษอัตโนมัติ | เป็น edge case ที่แก้มือได้ง่ายกว่าการันตีคุณภาพ transliteration อัตโนมัติ |

---

## 8. Open Questions

| คำถาม | ใครตอบ | บล็อกหรือไม่ |
|---|---|---|
| ยืนยันธีม navy+cream+pink เป็นตัวสุดท้าย เลิกใช้ธีมส้ม/ครีมเดิมหรือไม่ | P'Earth | บล็อก — ต้องรู้ก่อนเริ่ม build UI |
| ปุ่ม "Ask for a pattern" (AI ช่วยแนะนำการกรอก) เอาเข้า v1 หรือเลื่อนเป็น v1.1 | P'Earth | ไม่บล็อก — ทำทีหลังได้ |
| จะรัน blind test (proposal § 7) ก่อนหรือหลังสร้าง MVP | P'Earth | บล็อกการตัดสินใจลงทุนรอบถัดไป ไม่บล็อก MVP รอบแรก |
| เกณฑ์ "เสร็จ" ของ Workflow graph teaser คือแค่แสดงผลได้ หรือต้องลาก-เชื่อมได้จริงบางส่วน | Engineering | ไม่บล็อก MVP — ยืนยันก่อนเริ่มทำโมดูล Agentic ก็พอ |

---

## 9. Timeline Considerations

- ไม่มี deadline ผูกสัญญาหรือ compliance ในเอกสารที่มีอยู่ ณ ตอนนี้
- แนะนำลำดับ build: **Markdown Editor → Markdown workspace → Agentic module → Home** (Editor เป็นโมดูลเดี่ยวที่สุด เหมาะเริ่มก่อนเพื่อวางระบบ preview/toolbar ที่โมดูลอื่นใช้ต่อได้; Home ทำท้ายสุดเพราะเป็นหน้าการตลาดที่ไม่ผูกกับ logic หลัก)
- Blind test (proposal § 7) ควรอยู่ก่อนการลงทุนสร้างมุมมอง Canvas เต็มรูปแบบในเฟสถัดไป
